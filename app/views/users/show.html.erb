<% provide(:title, @user.name) %>
<div class = "row">
    <aside calss = "col-md-4">
        <section calss = "user_info">
            <h1>
            <%= gravatar_for @user %>
            <%= @user.name %>
            </h1>
        </section>
    </aside>
</div>
<%= @user.email %>, <%= @user.created_at %>, <%= @user.updated_at %>,<%= Time.now%>